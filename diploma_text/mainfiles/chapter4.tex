\chapter{Численные результаты и сравнение подходов}

В данной главе представлены результаты вычислительных экспериментов для модели реального делового цикла, реализованных на основе построенного в предыдущей главе численного алгоритма. Основная цель главы --- показать, как построенное глобальное решение (метод стрельбы) описывает динамику модели и чем оно отличается от локального приближения.

\section{Что было проделано в вычислительной части}

В рамках практической части работы выполнены следующие шаги:
\begin{enumerate}
    \item реализована численная схема решения детерминированной RBC-модели с подбором начального потребления \(C_0\) методом стрельбы;
    \item для каждого шага по времени реализовано решение неявного уравнения Эйлера для \(C_{t+1}\) методом Ньютона;
    \item рассчитано стационарное состояние модели и построены переходные траектории из разных начальных условий;
    \item построены фазовые графики в координатах \((K_t, C_t)\) и \((K_t, L_t)\);
    \item построены трехмерные траектории для наглядного анализа выхода экономики к стационарной точке;
    \item выполнено визуальное сравнение локального и глобального решений для DSGE-постановки.
\end{enumerate}

Во всех экспериментах использовались параметры:
\[
\beta = 0.96,\quad
\alpha = 0.33,\quad
\delta = 0.1,\quad
\sigma = 1,\quad
\phi = 0.75,\quad
\psi = 0.35.
\]

\section{Результаты решения детерминированной модели}

\subsection{Фазовые траектории}

На рис.~\ref{fig:rbc_phase} приведены фазовые траектории для детерминированной модели. Левый график показывает совместную динамику капитала и потребления \((K_t, C_t)\), правый --- капитала и труда \((K_t, L_t)\).

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{rbc_solution_kc.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{rbc_solution_kl.png}
    \end{minipage}
    \caption{Фазовые траектории детерминированной RBC-модели: \((K_t, C_t)\) и \((K_t, L_t)\)}
    \label{fig:rbc_phase}
\end{figure}

Полученные графики подтверждают наличие устойчивой переходной траектории к стационарному состоянию. Подбор \(C_0\) методом стрельбы обеспечивает сходящуюся динамику без взрывного поведения переменных.

\subsection{Трехмерные траектории}

Для дополнительной проверки устойчивости построены трехмерные траектории из двух различных начальных условий по капиталу. На рис.~\ref{fig:rbc_3d} показаны траектории, соответствующие начальным значениям \(K_0=0.8065\) и \(K_0=32.2582\).

\begin{figure}[htbp]
    \centering
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{rbc_trajectory_3d_0.8065.png}
    \end{minipage}
    \hfill
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{rbc_trajectory_3d_32.2582.png}
    \end{minipage}
    \caption{Трехмерные переходные траектории для разных начальных значений капитала}
    \label{fig:rbc_3d}
\end{figure}

Обе траектории сходятся к одной и той же стационарной точке, что является ожидаемым результатом для седлово-устойчивой динамической системы в окрестности равновесия.

\section{Сравнение локального и глобального решения DSGE}

На рис.~\ref{fig:local_global_dsge} приведено сравнение локального и глобального подходов.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.9\textwidth]{local_global_dsge.png}
    \caption{Сравнение локального и глобального решений DSGE}
    \label{fig:local_global_dsge}
\end{figure}

Локальное решение, получаемое через линеаризацию вокруг стационарного состояния, хорошо работает в малой окрестности равновесия и отличается вычислительной простотой. Глобальное решение учитывает нелинейность модели на всем диапазоне состояний и более корректно описывает динамику при больших отклонениях от стационарной точки.

Практический вывод состоит в следующем: для быстрого анализа малых шоков локальные методы удобны, но для задач, связанных с существенными отклонениями и исследованием полной переходной динамики, предпочтителен глобальный подход.

\section{Выводы}

Вычислительные эксперименты подтвердили корректность реализованной схемы. Метод стрельбы позволил построить устойчивые переходные траектории детерминированной модели, а сравнение с локальным DSGE-приближением показало границы применимости каждого из методов. Полученные результаты формируют основу для дальнейшего расширения модели, в том числе для учета монетарной политики и анализа импульсных откликов в стохастической постановке.
