% !TeX program = pdflatex
\documentclass[12pt,a4paper]{report}   % 12pt вместо 14pt

\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}
\usepackage{geometry}
\usepackage{setspace}
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{booktabs}
\usepackage{float}
\usepackage[unicode]{hyperref}
\usepackage{indentfirst}
\usepackage{enumitem}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{longtable}
\usepackage{xcolor}
\usepackage{tempora}
\usepackage{tocloft}

% Поля: левое 30 мм, правое 15 мм, верх/низ 20 мм
\geometry{left=30mm,right=15mm,top=20mm,bottom=20mm}
\onehalfspacing                     % полуторный интервал
\setlength{\parindent}{1.25cm}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    urlcolor=blue,
    citecolor=black
}

% Настройка заголовков глав и разделов
\titleformat{\chapter}[block]
    {\normalfont\bfseries\centering}
    {\thechapter.}
    {1em}
    {}
\titlespacing*{\chapter}{0pt}{-35pt}{12pt}

\titleformat{\section}
    {\normalfont\bfseries}
    {\thesection.}
    {1em}
    {}

\titleformat{\subsection}
    {\normalfont\bfseries}
    {\thesubsection.}
    {1em}
    {}

\titleformat{name=\subsection,numberless}
    {\normalfont\bfseries}
    {}
    {0pt}
    {}

\captionsetup{labelsep=period}     % точка после номера рисунка/таблицы

% Оглавление
\renewcommand{\contentsname}{Содержание}
\setlength{\cftbeforetoctitleskip}{-2.5em}
\setlength{\cftaftertoctitleskip}{1.5em}
\renewcommand{\cfttoctitlefont}{\bfseries\large}
\renewcommand{\cftaftertoctitle}{}

% Список литературы
\addto\captionsrussian{\renewcommand{\bibname}{Список литературы}}

\begin{document}

\begin{titlepage}
    \begin{center}
    \vspace*{0.2cm}
    \includegraphics[width=6.6cm]{images/msu_logo.png}
    \vspace{0.2cm}

    {\fontsize{12pt}{14pt}\selectfont
    МОСКОВСКИЙ ГОСУДАРСТВЕННЫЙ УНИВЕРСИТЕТ ИМЕНИ М.\,В.~ЛОМОНОСОВА\par}
    {\fontsize{12pt}{14pt}\selectfont
    ФАКУЛЬТЕТ ВЫЧИСЛИТЕЛЬНОЙ МАТЕМАТИКИ И КИБЕРНЕТИКИ\par}
    {\fontsize{12pt}{14pt}\selectfont
    КАФЕДРА ВЫЧИСЛИТЕЛЬНЫХ МЕТОДОВ\par}

    \vspace{2.5cm}

    {\fontsize{15pt}{17pt}\selectfont
    Козлов Кирилл Андреевич\par}

    \vspace{0.25cm}

    {\bfseries\fontsize{16pt}{18pt}\selectfont
    Численное решение модели реального делового цикла\par
    для закрытой экономики\par}

    \vspace{1.6cm}

    {\fontsize{16pt}{18pt}\selectfont
    ВЫПУСКНАЯ КВАЛИФИКАЦИОННАЯ РАБОТА\par}
    \end{center}

    \vspace{1.6cm}
    \noindent
    \hspace*{6.0cm}
    \begin{minipage}{0.43\textwidth}
    {\bfseries\fontsize{15pt}{17pt}\selectfont
    Научный руководитель:\par}
    \vspace{0.15cm}
    {\fontsize{14pt}{16pt}\selectfont
    профессор, д.ф.-м.н.\par
    Богомолов Сергей Владимирович\par}
    \end{minipage}
    \vfill
    \begin{center}
    {\fontsize{15pt}{17pt}\selectfont Москва, 2026}
    \end{center}
    \vspace*{-0.8cm}
\end{titlepage}

% Начинаем нумерацию страниц с оглавления (стр. 2)
\setcounter{page}{2}
\pagenumbering{arabic}

\tableofcontents
\clearpage


% --- Введение ---
\chapter*{Введение}
    \addcontentsline{toc}{chapter}{Введение}

    Модели динамического общего равновесия занимают важное место в современной макроэкономической теории. Они позволяют исследовать совместную динамику выпуска, потребления, капитала и труда, а также анализировать механизм межвременного выбора экономических агентов. Одной из наиболее известных конструкций такого типа является модель реального делового цикла (Real Business Cycle, RBC), в которой колебания основных макроэкономических переменных объясняются реакцией экономики на реальные шоки при рациональном поведении домохозяйств и фирм.

    Актуальность темы обусловлена тем, что даже в детерминированной постановке модель реального делового цикла приводит к нелинейной системе разностных уравнений, для которой аналитическое построение траектории, вообще говоря, невозможно. В связи с этим ключевую роль приобретают численные методы. Особый интерес представляет случай, когда в уравнении Эйлера доходность капитала зависит от величин следующего периода. Тогда переход по потреблению определяется неявно, и на каждом шаге численного алгоритма требуется решать дополнительное нелинейное уравнение.

    Целью выпускной квалификационной работы является построение и численное исследование детерминированной модели реального делового цикла для закрытой экономики, нахождение ее стационарного состояния и реализация численного решения методом стрельбы.

    Для достижения поставленной цели необходимо решить следующие задачи:
    \begin{enumerate}[label=\arabic*)]
        \item выписать систему уравнений модели и свести ее к замкнутой динамической форме;
        \item аналитически получить формулы для стационарного состояния;
        \item рассмотреть существующие численные методы решения задач оптимального управления в макроэкономике;
        \item реализовать численный алгоритм решения методом стрельбы;
        \item построить переходные траектории основных макроэкономических переменных;
        \item исследовать чувствительность решения к выбору параметров модели.
    \end{enumerate}

    Объектом исследования является детерминированная макроэкономическая модель закрытой экономики типа RBC. Предметом исследования выступают стационарное состояние модели, переходная динамика капитала, потребления, выпуска и труда, а также численные методы построения устойчивой траектории.

    Работа состоит из введения, пяти глав, заключения и списка литературы. В первой главе формально ставится задача и выписывается система уравнений модели. Во второй главе рассматриваются существующие численные подходы к решению задач оптимального управления в экономике. В третьей главе проводится аналитическое исследование стационарного состояния и выводится двумерная динамическая система. В четвертой главе описывается программная реализация численного метода и приводятся результаты вычислительных экспериментов. В пятой главе формулируются основные выводы исследования.

% --- Глава 1 ---
\chapter{Постановка задачи}
    \section{Описание модели}
        Рассматривается детерминированная модель реального делового цикла в дискретном времени. Динамика экономики описывается следующей системой уравнений:
        \begin{equation}
        \frac{C_{t+1}}{C_t}=\beta\bigl(1+R_{t+1}-\delta\bigr),
        \end{equation}
        \begin{equation}
        K_{t+1}=I_t+(1-\delta)K_t,
        \end{equation}
        \begin{equation}
        Y_t=A K_t^{\alpha}L_t^{1-\alpha},
        \end{equation}
        \begin{equation}
        L_t=\left(\frac{w_t}{\phi C_t}\right)^{\frac{1}{\psi}},
        \end{equation}
        \begin{equation}
        R_t=\alpha\frac{Y_t}{K_t},
        \qquad
        w_t=(1-\alpha)\frac{Y_t}{L_t},
        \end{equation}
        \begin{equation}
        Y_t=C_t+I_t.
        \end{equation}

        Здесь $C_t$ обозначает потребление в момент времени $t$, $K_t$ --- запас капитала, $L_t$ --- предложение труда, $Y_t$ --- выпуск, $I_t$ --- инвестиции, $R_t$ --- доходность капитала, $w_t$ --- заработную плату. Параметр $\beta\in(0,1)$ является коэффициентом межвременного дисконтирования, $\alpha\in(0,1)$ --- эластичностью выпуска по капиталу, $A>0$ --- уровнем совокупной факторной производительности, $\delta\in(0,1)$ --- нормой выбытия капитала, $\phi>0$ --- параметром дезполезности труда, а $\psi>0$ --- параметром, связанным с эластичностью предложения труда по Фришу.

    \section{Формальная постановка задачи}

        Требуется исследовать динамику модели при заданных параметрах $\beta$, $\alpha$, $A$, $\delta$, $\phi$, $\psi$ и начальном значении капитала $K_0$. Для этого необходимо:

        \begin{enumerate}[label=\arabic*)]
            \item выразить переменные $L_t$, $Y_t$, $R_t$ через основные переменные состояния модели;
            \item получить стационарное состояние $(K^*,C^*,L^*,Y^*)$;
            \item построить переходную траекторию, сходящуюся к стационарной точке;
            \item реализовать численный алгоритм, позволяющий подбирать начальное значение $C_0$ для устойчивой траектории;
            \item провести вычислительные эксперименты для различных начальных условий.
        \end{enumerate}

    \section{Сведение системы к динамической форме}

        Из уравнений для заработной платы и предложения труда получаем
        \begin{equation}
        L_t^{\psi+\alpha}=\frac{(1-\alpha)A}{\phi}\frac{K_t^{\alpha}}{C_t},
        \end{equation}
        откуда
        \begin{equation}
        L_t=\left(\frac{(1-\alpha)A}{\phi}\frac{K_t^{\alpha}}{C_t}\right)^{\frac{1}{\psi+\alpha}}.
        \end{equation}

        Тогда выпуск и доходность капитала выражаются через $(K_t,C_t)$:
        \begin{equation}
        Y_t=A K_t^{\alpha}L_t^{1-\alpha},
        \qquad
        R_t=\alpha\frac{Y_t}{K_t}.
        \end{equation}

        После этого динамика капитала принимает вид
        \begin{equation}
        K_{t+1}=Y_t-C_t+(1-\delta)K_t,
        \end{equation}
        а переменная $C_{t+1}$ определяется из неявного соотношения
        \begin{equation}
        C_{t+1}=\beta\bigl(1+R(K_{t+1},C_{t+1})-\delta\bigr)C_t.
        \end{equation}

        Таким образом, модель сводится к двумерной системе по переменным $K_t$ и $C_t$, в которой переход по капиталу задается явно, а переход по потреблению --- неявно.

% --- Глава 2 ---
\chapter{Обзор существующих решений}

    \section{Подходы к получению динамической системы}

        В задачах оптимального управления в экономике необходимые условия оптимальности могут быть получены различными способами. Для моделей в дискретном времени естественным является секвенциальный подход, основанный на методе Лагранжа с динамическими множителями. Для моделей в непрерывном времени аналогичную роль играет принцип максимума Понтрягина. Наряду с этим существует рекурсивный подход, основанный на принципе оптимальности Беллмана и уравнении Беллмана.

        Перечисленные подходы позволяют получить систему уравнений, описывающую динамику модели, после чего возникает задача ее численного решения. В литературе принято разделять локальные и глобальные методы решения.

    \section{Локальные методы}

        К локальным методам относятся линеаризация или лог-линеаризация модели в окрестности стационарного состояния, а также методы, основанные на спектральном анализе матрицы перехода. В частности, для дискретных моделей рациональных ожиданий широко применяется метод Бланшара--Кана, позволяющий установить существование и единственность устойчивой траектории по расположению собственных значений матрицы линеаризованной системы.

        Преимущество локальных методов заключается в сравнительно малой вычислительной сложности и удобстве анализа устойчивости. Однако они описывают поведение модели только в окрестности стационарной точки и могут давать заметную погрешность при сильной нелинейности или больших отклонениях от равновесия.

    \section{Глобальные методы}

        К глобальным методам относятся метод стрельбы, сеточные методы, методы итерации по функции ценности и проекционные методы. Метод стрельбы строит траекторию задачи как решение краевой проблемы, подбирая недостающие начальные значения так, чтобы на конечном горизонте траектория попадала в окрестность стационарного состояния. Этот подход естественен для детерминированных моделей с седловой динамикой.

        Сеточные методы и методы итерации по функции ценности опираются на уравнение Беллмана и позволяют находить глобальное решение задачи, но обычно требуют существенно больших вычислительных затрат. Проекционные методы приближают искомые функции на конечномерных базисах и также применяются для решения нелинейных макроэкономических моделей.

    \section{Выбор метода для настоящей работы}

        В данной работе исследуется детерминированная RBC-модель с учетом труда и неявным переходом по потреблению. Для такой постановки метод стрельбы является естественным выбором, поскольку позволяет строить глобальную переходную траекторию и непосредственно учитывать нелинейность модели. Вместе с тем результаты сопоставляются с идеями локального анализа, основанного на исследовании стационарной точки и свойств линеаризованной системы.

% --- Глава 3 ---
\chapter{Исследование и построение решения задачи}
    \section{Аналитическое исследование стационарного состояния}

        В стационарном состоянии выполняются равенства
        \[
        K_{t+1}=K_t=K^*, \qquad
        C_{t+1}=C_t=C^*, \qquad
        L_{t+1}=L_t=L^*.
        \]

        Из уравнения Эйлера получаем
        \begin{equation}
        1=\beta(1+R^*-\delta),
        \end{equation}
        следовательно,
        \begin{equation}
        R^*=\frac{1}{\beta}-1+\delta. \tag{3.1}
        \end{equation}

        Из закона накопления капитала имеем
        \[
        K^*=Y^* - C^* + (1 -\delta)K^* \Rightarrow Y^*=C^*+\delta K^*.
        \]

        С учётом условия связи через предельный продукт капитала:
        \[
        R^*=\alpha\frac{Y^*}{K^*} = \alpha\left(\frac{C^*}{K^*} + \delta\right)
        \]
        получаем отношение:
        \begin{equation}
        \frac{C^*}{K^*}=\frac{R^*}{\alpha}-\delta \equiv v. \tag{3.2}
        \end{equation}

        Находим $L^*$:
        \[
        \phi C^*(L^*)^{\psi} = w^*, \quad w^* = (1-\alpha)\frac{Y^*}{L^*}, \quad Y^* = \frac{R^*}{\alpha}K^*, \quad C^* = vK^*.
        \]
        После подстановки и сокращений:
        \[
        \phi v (L^*)^{\psi+1} = \frac{1 - \alpha}{\alpha}R^*,
        \]
        откуда
        \begin{equation}
        L^*=\left(\frac{(1-\alpha)R^*}{\alpha\phi v}\right)^{\frac{1}{\psi+1}}. \tag{3.3}
        \end{equation}

        Капитал $K^*$ определяется из производственной функции:
        \[
        R^* = \alpha \frac{Y^*}{K^*}, \quad Y^* = A(K^*)^{\alpha}(L^*)^{1 - \alpha},
        \]
        следовательно,
        \begin{equation}
        K^*=\left(\frac{\alpha A (L^*)^{1-\alpha}}{R^*}\right)^{\frac{1}{1-\alpha}}. \tag{3.4}
        \end{equation}

        Потребление $C^*$:
        \[
        C^*= v K^*. \tag{3.5}
        \]

        Итого в стационарном состоянии имеем аналитические формулы для всех величин.

    \section{Численные значения стационарного состояния}

        В вычислительных экспериментах использовался следующий набор параметров:
        \begin{equation}
        \beta=0.95, \quad
        \alpha=0.33, \quad
        A=0.95, \quad
        \sigma=1.0, \quad
        \delta=0.1, \quad
        \phi=0.75, \quad
        \psi=0.35
        \end{equation}

        Для указанных параметров получены следующие значения стационарного состояния:
        \begin{equation}
        R^*=0.1416666667, \qquad
        K^*=3.6635846596, \qquad
        C^*=1.2063925243,
        \end{equation}
        \begin{equation}
        L^*=1.1195044626, \qquad
        Y^*=1.5727509902, \qquad
        I^*=0.3663584660.
        \end{equation}

        Следует отметить, что в данной калибровке стационарное значение труда превышает единицу. Это не препятствует формальному численному исследованию, однако указывает на необходимость дополнительного анализа чувствительности результатов к параметрам $\phi$, $\psi$ и $A$.

    \section{Метод стрельбы}
    
        Для нахождения устойчивой переходной траектории используется метод стрельбы. Его идея состоит в следующем: при фиксированном начальном значении капитала $K_0$ подбирается такое начальное значение потребления $C_0$, при котором траектория системы на конечном горизонте максимально приближается к стационарному состоянию.

        На каждом шаге алгоритма выполняются следующие действия:
        \begin{enumerate}[label=\arabic*)]
            \item по заданным $K_t$ и $C_t$ вычисляется $L_t$;
            \item определяется выпуск $Y_t$ и инвестиции $I_t=Y_t-C_t$;
            \item по уравнению накопления вычисляется капитал $K_{t+1}$;
            \item численно решается нелинейное уравнение относительно $C_{t+1}$:
            \begin{equation}
            C_{t+1}=\beta\bigl(1+R(K_{t+1},C_{t+1})-\delta\bigr)C_t;
            \end{equation}
            \item процесс повторяется до достижения заданного горизонта моделирования.
        \end{enumerate}

        Подбор начального потребления осуществляется по минимуму функционала, измеряющего отклонение терминального состояния от стационарной точки.
        
        \subsection{Построение траектории при фиксированном \texorpdfstring{$C_0$}{C0}}

            Пусть заданы начальные значения $K_0$ и $C_0$. Для каждого периода $t=0,1,\dots,T-1$ траектория строится итерационно. Сначала по внутрипериодному условию вычисляется труд:
            \[
            L_t=\left(\frac{(1-\alpha)A}{\phi}\frac{K_t^\alpha}{C_t}\right)^{\frac{1}{\psi+\alpha}}.
            \]
            Затем определяются выпуск и связанные с ним величины:
            \(Y_t=A K_t^\alpha L_t^{1-\alpha}\),
            \(R_t=\alpha\frac{Y_t}{K_t}\),
            \(I_t=Y_t-C_t\).
            После этого капитал следующего периода находится из закона накопления:
            \(K_{t+1}=Y_t-C_t+(1-\delta)K_t\).

            Значение $C_{t+1}$ определяется из уравнения Эйлера и находится как корень одномерного нелинейного уравнения
            \[
            f(c)=c-\beta\bigl(1+R(K_{t+1},c)-\delta\bigr)C_t=0,
            \]
            где \(c=C_{t+1}\).
            При этом функция доходности на шаге \(t+1\) задается через
            \(Y_{t+1}=A K_{t+1}^\alpha L_{t+1}^{1-\alpha}\),
            \(L_{t+1}=\left(\frac{(1-\alpha)A}{\phi}\frac{K_{t+1}^\alpha}{c}\right)^{\frac{1}{\psi+\alpha}}\),
            \(R_{t+1}=\alpha\frac{Y_{t+1}}{K_{t+1}}\).
            После нахождения \(C_{t+1}\) выполняется переход к периоду \(t+1\), и процедура повторяется до горизонта \(T\).
        \subsection{Метод Ньютона для нахождения \texorpdfstring{$C_{t+1}$}{C(t+1)}}

            После вычисления \(K_{t+1}\) значение \(C_{t+1}\) определяется из неявного уравнения Эйлера:
            \[
            f(c)=c-\beta\bigl(1+R(K_{t+1},c)-\delta\bigr)C_t=0,\qquad c=C_{t+1}.
            \]
            Для фиксированного \(K=K_{t+1}\) используем обозначения
            \(L(K,c)=\left(\frac{(1-\alpha)A}{\phi}\frac{K^\alpha}{c}\right)^{\frac{1}{\psi+\alpha}}\),
            \(Y(K,c)=A K^\alpha L(K,c)^{1-\alpha}\),
            \(R(K,c)=\alpha\frac{Y(K,c)}{K}\).

            Производная \(f'(c)\) вычисляется аналитически. Из
            \(\frac{\partial L}{\partial c}=-\frac{1}{\psi+\alpha}\frac{L}{c}\),
            \(\frac{\partial Y}{\partial c}=-\frac{1-\alpha}{\psi+\alpha}\frac{Y}{c}\)
            получаем
            \(\frac{\partial R}{\partial c}=-\frac{1-\alpha}{\psi+\alpha}\frac{R}{c}\), а значит
            \[
            f'(c)=1-\beta C_t\frac{\partial R(K,c)}{\partial c}
            =1+\beta C_t\frac{1-\alpha}{\psi+\alpha}\frac{R(K,c)}{c}.
            \]

            Тогда итерации Ньютона записываются в виде
            \[
            c^{(n+1)}=
            c^{(n)}-\frac{f(c^{(n)})}{f'(c^{(n)})}
            =
            c^{(n)}-
            \frac{c^{(n)}-\beta\bigl(1+R(K_{t+1},c^{(n)})-\delta\bigr)C_t}
            {1+\beta C_t \frac{1-\alpha}{\psi+\alpha}\frac{R(K_{t+1},c^{(n)})}{c^{(n)}}}.
            \]
            После достижения критерия сходимости принимается \(C_{t+1}=c^{(n)}\).
        
        \subsection{Псевдокод метода стрельбы}
            \begin{enumerate}[label=\arabic*)]
                \item Задать параметры модели $(\alpha,\beta,\delta,\phi,\psi,A)$ и вычислить стационарное состояние $(K^*,C^*,L^*)$.
                \item Зафиксировать $K_0$ и начальный интервал для потребления: $C_0\in[C_{\min},C_{\max}]$.
                \item Повторять внешнюю бисекцию по $C_0$, пока $|C_{\max}-C_{\min}|\ge\varepsilon_{\text{outer}}$.
                \item На каждой итерации положить $C_0=\frac{C_{\min}+C_{\max}}{2}$ и построить траекторию на горизонте $t=0,1,\dots,T-1$.
                \item Для каждого $t$ вычислить
                \(L_t=\left(\frac{(1-\alpha)A}{\phi}\frac{K_t^\alpha}{C_t}\right)^{\frac{1}{\psi+\alpha}}\),
                \(Y_t=A K_t^\alpha L_t^{1-\alpha}\),
                \(K_{t+1}=Y_t-C_t+(1-\delta)K_t\),
                затем найти $C_{t+1}$ методом Ньютона из
                \(f(c)=c-\beta\bigl(1+R(K_{t+1},c)-\delta\bigr)C_t=0\).
                \item По завершении траектории вычислить терминальное отклонение $g(C_0)=K_T-K^*$.
                \item Обновить интервал: если $g(C_0)>0$, то $C_{\min}=C_0$, иначе $C_{\max}=C_0$.
                \item После сходимости внешней бисекции принять найденное $C_0^*$ и построить итоговую saddle-path траекторию $(K_t,C_t,L_t)$.
            \end{enumerate}



% --- Глава 4 ---
\chapter{Описание практической части}
    \section{Программная реализация}

        Численная реализация выполнена на языке Python с использованием библиотек \texttt{NumPy}, \texttt{SciPy}, \texttt{Matplotlib} и \texttt{Plotly}. Библиотека \texttt{SciPy} использовалась для решения одномерного нелинейного уравнения и подбора стартового значения $C_0$. Библиотека \texttt{Matplotlib} применялась для построения статических графиков, а \texttt{Plotly} --- для интерактивной визуализации фазовых траекторий.

        Алгоритм реализован в виде набора функций, каждая из которых отвечает за вычисление отдельных компонент модели: труда, выпуска, доходности капитала, следующего значения капитала, а также решения неявного уравнения для потребления. Поверх этих функций построена процедура генерации траектории на конечном горизонте и процедура оптимального подбора начального значения потребления.

    \section{Эксперимент 1: старт ниже стационарного уровня капитала}

        В первом эксперименте рассматривалось начальное условие
        \begin{equation}
        K_0=0.3K^*.
        \end{equation}

        Методом стрельбы было найдено значение
        \begin{equation}
        C_0\approx 0.702956.
        \end{equation}

        При таком выборе начального потребления траектория переменных $K_t$, $C_t$, $L_t$ ,$Y_t$ сходится к стационарному состоянию. Экономически это соответствует ситуации, когда экономика стартует с недостаточным объемом капитала и постепенно выходит на долгосрочное равновесие.

            На рис.~\ref{fig:K_low_1},~\ref{fig:K_low_2},~\ref{fig:K_low_3d} показана динамика основных показателей при старте ниже стационарного уровня ($K_0=0.3K^*.$).
            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_2d_K0_1.0991_T_45.png}
                \caption{Переходная динамика $K_t,Y_t,C_t,L_t$ при $K_0<K^*$}
                \label{fig:K_low_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_2d_additional_K0_1.0991_T_45.png}
                \caption{Переходная динамика $R_t,w_t,I_t$ при $K_0<K^*$}
                \label{fig:K_low_2}
            \end{figure}


            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_3d_1.0991.png}
                \caption{Трёхмерная фазовая траектория модели при $K_0<K^*$}
                \label{fig:K_low_3d}
            \end{figure}
    \section{Эксперимент 2: старт выше стационарного уровня капитала}

        Во втором эксперименте использовалось начальное условие
        \begin{equation}
        K_0=10K^*.
        \end{equation}

        Подбором методом стрельбы было найдено значение
        \begin{equation}
        C_0\approx 4.542463.
        \end{equation}

        В этом случае экономика стартует с избыточным объемом капитала по сравнению со стационарным уровнем. Численные расчеты показывают, что и в данной ситуации траектория постепенно приближается к steady state.


            На рис.~\ref{fig:K_high_1},~\ref{fig:K_high_2},~\ref{fig:K_high_3d} показана динамика основных показателей при старте выше стационарного уровня ($K_0=10K^*.$).
            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_2d_K0_36.6358_T_56.png}
                \caption{Переходная динамика $K_t,Y_t,C_t,L_t$ при $K_0>K^*$}
                \label{fig:K_high_1}
            \end{figure}

            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_2d_additional_K0_36.6358_T_56.png}
                \caption{Переходная динамика $R_t,w_t,I_t$ при $K_0>K^*$}
                \label{fig:K_high_2}
            \end{figure}


            \begin{figure}[H]
                \centering
                \includegraphics[width=\textwidth]{images/rbc_trajectory_3d_36.6358.png}
                \caption{Трёхмерная фазовая траектория модели при $K_0>K^*$}
                \label{fig:K_high_3d}
            \end{figure}

    \section{Оценка вычислительной схемы}

        Проведенные вычислительные эксперименты показывают, что метод стрельбы позволяет устойчиво строить переходные траектории для рассматриваемой модели. Точность результата зависит от способа решения неявного уравнения для $C_{t+1}$, длины горизонта моделирования и выбора критерия близости к стационарному состоянию. При этом учет труда делает модель более содержательной, но одновременно усложняет нелинейную структуру динамики.

% --- Выводы (по требованию) ---
\chapter*{Выводы}
    \addcontentsline{toc}{chapter}{Выводы}
    В результате выполнения работы получены следующие основные результаты:
    \begin{enumerate}[label=\arabic*)]
        \item аналитически выведены формулы для стационарного состояния детерминированной RBC-модели с трудом;
        \item проведён обзор локальных и глобальных численных методов решения задач оптимального управления в макроэкономике;
        \item реализован численный алгоритм метода стрельбы с учётом неявного уравнения Эйлера для потребления;
        \item выполнены вычислительные эксперименты для двух начальных уровней капитала (ниже и выше стационарного);
        \item построены графики переходных траекторий и фазовые портреты, подтверждающие сходимость к стационарному состоянию.
    \end{enumerate}

% --- Заключение (по желанию) ---
\chapter*{Заключение}
    \addcontentsline{toc}{chapter}{Заключение}
    Проведённое исследование показало, что численные результаты чувствительны к параметрам, определяющим предложение труда. Перспективным направлением дальнейшей работы является более точная калибровка параметров на основе экономически интерпретируемых ограничений, а также расширение модели за счёт включения технологических шоков, применения локальных методов решения и анализа стохастической версии RBC-модели.

\clearpage
\phantomsection
\addcontentsline{toc}{chapter}{Список литературы}
    \begin{thebibliography}{99}

    \bibitem{romer}
    Бессонов В. А. Проблемы построения производственных функций в российской переходной экономике. — Институт экономики переходного периода, 2002.

    \bibitem{acemoglu}
    Acemoglu D. Introduction to Modern Economic Growth. — Princeton: Princeton University Press, 2009.

    \bibitem{heermaussner}
    Heer B., Maussner A. Dynamic General Equilibrium Modeling: Computational Methods and Applications. — Berlin: Springer, 2009.

    \bibitem{ljungqvist}
    Ljungqvist L., Sargent T. Recursive Macroeconomic Theory. — Cambridge, MA: MIT Press, 2018.

    \bibitem{miranda}
    Miranda M., Fackler P. Applied Computational Economics and Finance. — Cambridge, MA: MIT Press, 2002.

    \bibitem{judd}
    Judd K. Numerical Methods in Economics. — Cambridge, MA: MIT Press, 1998.

    \bibitem{kingrebello}
    King R., Rebelo S. Resuscitating Real Business Cycles. // Handbook of Macroeconomics. — Elsevier, 1999.

    \bibitem{cooley}
    Cooley T. (ed.) Frontiers of Business Cycle Research. — Princeton University Press, 1995.

    \end{thebibliography}

\end{document}